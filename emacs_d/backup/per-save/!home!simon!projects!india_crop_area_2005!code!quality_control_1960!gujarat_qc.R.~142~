## Author : Simon Moulds
## Date   : Apr 2018

## Quality control for Gujarat

## Barley
## ######

xx[["apy_barley"]] = xx[["irr_barley"]]

## Chickpea
## ########

xx[["apy_chickpea"]] %<>% locf_interp(61:63)
xx[["irr_chickpea"]] %<>% locf_interp(61:63)

if (dist_nm %in% c("Ahmadabad")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 21:38, 39)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41:50, 51)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Amreli.Bhavnagar.Rajkot")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41:50, 40)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Banas Kantha")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41, 40)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Bharuch")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 21:53, 54)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Jamnagar")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 38:50, 51)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Junagadh")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41, 40)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Kachchh")) {
    xx[["irr_chickpea"]] = xx[["apy_chickpea"]]
}

if (dist_nm %in% c("Kheda")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 21:36, 18)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 39:41, 38)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 43:50, 42)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Mahesana")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 21:39, 18)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41, 40)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Panch Mahals")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41, 40)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Sabar Kantha")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 40:41, 39)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Surat")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 21, 22)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 23:26, 22)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 28, 27)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 30:50, 29)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("Surendranagar")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 23:32, 22)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 41, 40)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

if (dist_nm %in% c("The Dangs")) {
    xx[["irr_chickpea"]] = 0
}

if (dist_nm %in% c("Vadodara")) {
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 21, 22)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 25:28, 24)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 32:34, 31)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 36, 35)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 39:42, 38)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)
    xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 59:60, 58)
}

## Cotton
## ######

xx[["apy_cotton"]] %<>% locf_interp(61:63)
xx[["irr_cotton"]] %<>% locf_interp(61:63)

xx[["irr_cotton"]] %<>% irr_frac_interp(xx[["irr_cotton"]], 48, 47)

if (dist_nm %in% c("Amreli.Bhavnagar.Rajkot","Banas Kantha","Bharuch","Jamnagar","Junagadh","Kachchh","Kheda","Panch Mahals","Sabar Kantha","Surat","Surendranagar","Vadodara")) {
    xx[["irr_cotton"]] %<>% irr_frac_interp(xx[["apy_cotton"]], 45, 44)
    xx[["irr_cotton"]] %<>% irr_frac_interp(xx[["apy_cotton"]], 48, 47)
    xx[["irr_cotton"]] %<>% irr_frac_interp(xx[["apy_cotton"]], 59:60, 58)
}

if (dist_nm %in% c("Mahesana")) {
    xx[["apy_cotton"]] %<>% tot_frac_interp(xx[["irr_cotton"]], 38:39, 37)
    xx[["irr_cotton"]] %<>% irr_frac_interp(xx[["apy_cotton"]], 48, 47)
    xx[["irr_cotton"]] %<>% irr_frac_interp(xx[["apy_cotton"]], 59:60, 58)
}
    
## Finger millet
## #############

xx[["apy_finger_millet"]] %<>% locf_interp(61:63)

if (!dist_nm %in% c("Kheda","Panch Mahals","Sabar Kantha","Surat","The Dangs")) {
    xx[["apy_finger_millet"]] = 0
    xx[["irr_finger_millet"]] = 0
} else {
    if (dist_nm %in% c("The Dangs")) {
        xx[["apy_finger_millet"]] %<>% linear_interp(44)
    }
}

## Groundnut
## #########

xx[["apy_groundnut"]] %<>% locf_interp(61:63)
xx[["irr_groundnut"]] %<>% irr_frac_interp(xx[["apy_groundnut"]], 59:61, 58)

xx[["irr_groundnut"]] %<>% irr_frac_interp(xx[["apy_groundnut"]], 45, 44)
xx[["irr_groundnut"]] %<>% irr_frac_interp(xx[["apy_groundnut"]], 48, 47)

if (dist_nm %in% c("Vadodara")) {
    xx[["irr_groundnut"]] %<>% irr_frac_interp(xx[["apy_groundnut"]], 50, 49)
}

if (dist_nm %in% c("Surat")) {
    xx[["irr_groundnut"]] %<>% irr_frac_interp(xx[["apy_groundnut"]], 58:61, 57)
}

## Maize
## #####

xx[["apy_maize"]] %<>% locf_interp(61:63)
xx[["irr_maize"]] %<>% irr_frac_interp(xx[["apy_maize"]], 59:61, 58)

if (dist_nm %in% c("Ahmadabad")) {
    xx[["irr_maize"]] %<>% irr_frac_interp(xx[["apy_maize"]], 37, 36)
    xx[["irr_maize"]] %<>% irr_frac_interp(xx[["apy_maize"]], 55, 54)    
}

if (dist_nm %in% c("Kachchh","Surendranagar")) {
    xx[["apy_maize"]] = 0
    xx[["irr_maize"]] = 0
}

if (dist_nm %in% c("Mahesana")) {
    xx[["irr_maize"]] %<>% irr_frac_interp(xx[["apy_maize"]], 55, 54)
}

## Pearl millet
## ############

xx[["apy_pearl_millet"]] %<>% locf_interp(61:63)

xx[["irr_pearl_millet"]] %<>% irr_frac_interp(xx[["apy_pearl_millet"]], 41, 40)
xx[["irr_pearl_millet"]] %<>% irr_frac_interp(xx[["apy_pearl_millet"]], 44:50, 43)
xx[["irr_pearl_millet"]] %<>% irr_frac_interp(xx[["apy_pearl_millet"]], 59:61, 58)

if (dist_nm %in% c("Amreli.Bhavnagar.Rajkot")) {
    xx[["irr_pearl_millet"]] %<>% irr_frac_interp(xx[["apy_pearl_millet"]], 27:29, 26)
}

## Pigeonpea
## #########

xx[["apy_pigeonpea"]] %<>% locf_interp(61:63)
xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 41, 40)
xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 44:50, 43)
xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 59:61, 58)

if (dist_nm %in% c("Amreli.Bhavnagar.Rajkot")) {
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 21:38, 39)
}

if (dist_nm %in% c("Banas Kantha","Panch Mahals")) {
    xx[["irr_pigeonpea"]] = 0
}

if (dist_nm %in% c("Jamnagar")) {
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 55:56, 54)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 58:61, 57)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 21:42, 43)
}

if (dist_nm %in% c("Junagadh")) {
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 21:42, 43)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 59:61, 58)
}

if (dist_nm %in% c("Kachchh","Surendranagar")) {
    xx[["apy_pigeonpea"]] = 0
    xx[["irr_pigeonpea"]] = 0
}

if (dist_nm %in% c("Kheda")) {
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 21:22, 23)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 24:35, 23)
}

if (dist_nm %in% c("Mahesana")) {
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 21:22, 23)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 24:41, 23)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 43:50, 42)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 56, 55)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 58:61, 57)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 51:52, 50)
}

if (dist_nm %in% c("Sabar Kantha")) {
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 21:23, 24)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 25:41, 24)
    xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 55:56, 54)
}

## Rapeseed
## ########

xx[["apy_rapeseed"]] %<>% locf_interp(61:63)
xx[["irr_rapeseed"]] %<>% irr_frac_interp(xx[["apy_rapeseed"]], 59:61, 58)
xx[["irr_rapeseed"]] %<>% irr_frac_interp(xx[["apy_rapeseed"]], 45, 44)
xx[["irr_rapeseed"]] %<>% irr_frac_interp(xx[["apy_rapeseed"]], 48, 49)

if (dist_nm %in% c("Mahesana")) {
    xx[["irr_rapeseed"]] %<>% irr_frac_interp(xx[["apy_rapeseed"]], 43, 42)
    xx[["irr_rapeseed"]] %<>% irr_frac_interp(xx[["apy_rapeseed"]], 45, 44)
}

if (dist_nm %in% c("Bharuch","Surat")) {
    xx[["apy_rapeseed"]] = 0
    xx[["irr_rapeseed"]] = 0
}

## Rice
## ####

xx[["irr_rice-winter"]] = xx[["irr_rice-autumn"]]
xx[["irr_rice-autumn"]] = 0

xx[["apy_rice"]] %<>% locf_interp(61:63)
xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 59:61, 58)

xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 41, 40)
xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 44:50, 43)

if (dist_nm %in% c("Banas Kantha")) {
    xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 22, 21)
    xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 25:34, 24)
    xx[["apy_rice"]] %<>% tot_frac_interp(xx[["irr_rice-winter"]], 36:40, 35)
}

if (dist_nm %in% c("Surat","Kheda")) {
    xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 41, 40)
    xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 44:50, 43)
    xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 59:61, 58)
    xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 21:30, 31)
}

if (dist_nm %in% c("Banas Kantha")) {
    stop()
    inf_ix = which(is.infinite(xx[["irr_rice-winter"]]))
    if (length(inf_ix) > 0) {
        xx[["irr_rice-winter"]] %<>% locf_interp(inf_ix)
    }
}

## Sesamum
## #######

xx[["apy_sesamum"]] %<>% locf_interp(61:63)
xx[["irr_sesamum"]] %<>% irr_frac_interp(xx[["apy_sesamum"]], 59:61, 58)
xx[["irr_sesamum"]] %<>% irr_frac_interp(xx[["apy_sesamum"]], 45, 44)
xx[["irr_sesamum"]] %<>% irr_frac_interp(xx[["apy_sesamum"]], 48, 47)

if (dist_nm %in% c("The Dangs")) {
    xx[["apy_sesamum"]] = 0
    xx[["irr_sesamum"]] = 0
}

## Sorghum
## #######

xx[["apy_sorghum-kharif"]] %<>% locf_interp(61:63)
xx[["apy_sorghum-rabi"]] %<>% locf_interp(61:63)

if (dist_nm %in% c("Ahmadabad","Amreli.Bhavnagar.Rajkot","Banas Kantha","Bharuch","Junagadh","Kheda")) {
    xx[["apy_sorghum-kharif"]] %<>% linear_interp(45:46)
}

## Sugarcane
## #########

xx[["apy_sugarcane-whole_year"]] %<>% locf_interp(61:63)
xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 59:61, 58)

xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 45, 44)
xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 48, 47)
xx[["irr_sugarcane-whole_year"]] %<>% pmin(xx[["apy_sugarcane-whole_year"]])

if (dist_nm %in% c("Surat")) {
    xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 42:43, 42)
}

## Wheat
## #####

xx[["apy_wheat"]] %<>% locf_interp(61:63)
xx[["irr_wheat"]] %<>% irr_frac_interp(xx[["apy_wheat"]], 59:61, 58)

if (dist_nm %in% c("Ahmadabad")) {
    xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 38, 37)
}

if (dist_nm %in% c("Banas Kantha")) {
    xx[["irr_wheat"]] %<>% tot_frac_interp(xx[["apy_wheat"]], 46, 45)
}

if (dist_nm %in% c("Banas Kantha","Mahesana")) {
    xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 37:40, 36)
}

if (dist_nm %in% c("Panch Mahals","Sabar Kantha")) {
    xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 38, 37)
}

if (dist_nm %in% c("Surat")) {
    xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 38:40, 37)
    xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 56:57, 55)
}

if (dist_nm %in% c("Vadodara")) {
    xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 56, 55)
}

