
cdef extern from "c_surf_evap.h":
    void c_surf_evap(double* es_pot, double* es_act, double* surface_storage, double* rew, double* w_surf, double* w_stage_two, double* evap_z, double* evap_z_min, int n_farm, int n_crop, int n_cell)

def f(double[:,:,:] es_pot, double[:,:,:] es_act, double[:,:,:] surface_storage, double[:,:,:] rew, double[:,:,:] w_surf, double[:,:,:] w_stage_two, double[:,:,:] evap_z, double[:,:,:] evap_z_min, int n_farm, int n_crop, int n_cell):
def f(es_pot, es_act, surface_storage, rew, w_surf, w_stage_two, evap_z, evap_z_min, n_farm, n_crop, n_cell):

    cdef double[:,:,::1] es_pot_memview = es_pot
    cdef double[:,:,::1] es_act_memview = es_act
    cdef double[:,:,::1] surface_storage_memview = surface_storage
    cdef double[:,:,::1] rew_memview = rew
    cdef double[:,:,::1] w_surf_memview = w_surf
    cdef double[:,:,::1] w_stage_two_memview = w_stage_two
    cdef double[:,:,::1] evap_z_memview = evap_z
    cdef double[:,:,::1] evap_z_min_memview = evap_z_min
    
    c_surf_evap(&es_pot_memview[0], &es_act_memview[0], &surface_storage_memview[0], &rew_memview[0], &w_surf_memview[0], &w_stage_two_memview[0], &evap_z_memview[0], &evap_z_min_memview[0], n_farm, n_crop, n_cell)
    
