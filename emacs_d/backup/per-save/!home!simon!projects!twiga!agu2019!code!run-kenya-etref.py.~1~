#!/usr/bin/env python
# coding: utf-8

import os
import glob

run_dir = "../data/etref_ghana"

def main():
    for config_file in glob.glob(run_dir + '/config/*.ini'):
        cmd = 'python -m etref.run ' + config_file
        os.system(cmd)

if __name__ == '__main__':
    main()
