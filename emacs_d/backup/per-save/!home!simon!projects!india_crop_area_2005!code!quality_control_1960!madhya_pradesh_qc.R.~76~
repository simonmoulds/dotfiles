## Author : Simon Moulds
## Date   : Apr 2018

## Quality control for Madhya Pradesh

## Barley
## ######

xx[["irr_barley"]] %<>% linear_interp(62)
xx[["apy_barley"]] %<>% tot_frac_interp(xx[["irr_barley"]], 61:63, 60)
xx[["apy_barley"]] %<>% tot_frac_interp(xx[["irr_barley"]], 49, 48)

if (dist_nm %in% c("Surguja")) {
    xx[["apy_barley"]] %<>% tot_frac_interp(xx[["irr_barley"]], 27, 26)
}

if (dist_nm %in% c("Bilaspur.Durg","Surguja")) {
    xx[["apy_barley"]] %<>% tot_frac_interp(xx[["irr_barley"]], 49:50, 48)
}

if (dist_nm %in% c("Rajgarh","Sidhi")) {
    xx[["apy_barley"]] %<>% tot_frac_interp(xx[["irr_barley"]], 61:63, 59)
}

## Chickpea
## ########

xx[["irr_chickpea"]] %<>% linear_interp(61)
xx[["apy_chickpea"]] %<>% tot_frac_interp(xx[["irr_chickpea"]], 61:63, 60)
xx[["irr_chickpea"]] %<>% irr_frac_interp(xx[["apy_chickpea"]], 44:50, 43)

## Cotton
## ######

xx[["irr_cotton"]] %<>% linear_interp(61)
xx[["apy_cotton"]] %<>% tot_frac_interp(xx[["irr_cotton"]], 61:63, 60)

## Finger millet
## #############

xx[["apy_finger_millet"]] %<>% locf_interp(61:63)
xx[["apy_finger_millet"]] %<>% linear_interp(41:47)
xx[["apy_finger_millet"]] %<>% linear_interp(49)

if (dist_nm %in% c("Surguja")) {
    xx[["apy_finger_millet"]] %<>% linear_interp(33)
}

## Groundnut
## #########

if (is.na(xx[["irr_groundnut"]][61])) {
    xx[["apy_groundnut"]] %<>% locf_interp(61:63)
    xx[["irr_groundnut"]] %<>% locf_interp(61:63)
} else {
    xx[["irr_groundnut"]] %<>% linear_interp(62)
    xx[["apy_groundnut"]] %<>% tot_frac_interp(xx[["irr_groundnut"]], 61:63, 60)
}

## Linseed
## #########

if (is.na(xx[["irr_linseed"]][61])) {
    xx[["apy_linseed"]] %<>% locf_interp(61:63)
    xx[["irr_linseed"]] %<>% locf_interp(61:63)
} else {
    xx[["irr_linseed"]] %<>% linear_interp(62)
    xx[["apy_linseed"]] %<>% tot_frac_interp(xx[["irr_linseed"]], 61:63, 60)
}

## Maize
## #####

if (is.na(xx[["irr_maize"]][61])) {
    xx[["apy_maize"]] %<>% locf_interp(61:63)
    xx[["irr_maize"]] %<>% locf_interp(61:63)
} else {
    xx[["irr_maize"]] %<>% linear_interp(62)
    xx[["apy_maize"]] %<>% tot_frac_interp(xx[["irr_maize"]], 61:63, 60)
}

## Pearl millet
## ############

xx[["apy_pearl_millet"]] %<>% locf_interp(61:63)
xx[["irr_pearl_millet"]] = 0

## Pigeonpea
## #########

if (is.na(xx[["irr_pigeonpea"]][61])) {
    xx[["apy_pigeonpea"]] %<>% locf_interp(61:63)
    xx[["irr_pigeonpea"]] %<>% locf_interp(61:63)
} else {
    xx[["irr_pigeonpea"]] %<>% linear_interp(62)
    xx[["apy_pigeonpea"]] %<>% tot_frac_interp(xx[["irr_pigeonpea"]], 61:63, 60)
}

xx[["irr_pigeonpea"]] %<>% irr_frac_interp(xx[["apy_pigeonpea"]], 44:50, 43)

## Rapeseed
## ########

xx[["irr_rapeseed"]] %<>% linear_interp(62)
xx[["apy_rapeseed"]] %<>% tot_frac_interp(xx[["irr_rapeseed"]], 61:63, 60)

## Rice
## ####

xx[["irr_rice-autumn"]][18:24] = xx[["irr_rice-winter"]][18:24]
xx[["irr_rice-winter"]] = xx[["irr_rice-autumn"]]
xx[["irr_rice-autumn"]] = 0

## xx[["irr_rice-autumn"]] %<>% linear_interp(62)
## xx[["irr_rice-winter"]] %<>% linear_interp(62)
## xx[["apy_rice"]] %<>% tot_frac_interp(rowSums(data.frame(xx[["irr_rice-autumn"]], xx[["irr_rice-winter"]],xx[["irr_rice-summer"]]),na.rm=TRUE), 61:63, 60)
xx[["apy_rice"]] %<>% locf_interp(61)
xx[["irr_rice-winter"]] %<>% locf_interp(61)
xx[["irr_rice-summer"]] %<>% locf_interp(61)

xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 44:50, 43)
xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 44:50, 43)

if (dist_nm %in% c("Bhind","Gwalior.Datia")) {
    xx[["irr_rice-winter"]] = xx[["apy_rice"]]
}

if (dist_nm %in% c("Rajgarh")) {
    xx[["irr_rice-winter"]] = xx[["apy_rice"]]
}

if (dist_nm %in% c("Jabalpur")) {
    xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 21:35, 36)
}

if (dist_nm %in% c("Balaghat","Bastar","Bilaspur.Durg","Jabalpur","Raigarh","Raipur")) {
    xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 33:35, 32)
}

if (dist_nm %in% c("Balaghat","Bastar","Bhind","Damoh")) {
    xx[["irr_rice-winter"]] %<>% irr_frac_interp(xx[["apy_rice"]], 54, 53)
}

if (dist_nm %in% c("Balaghat")) {
    xx[["irr_rice-summer"]] %<>% irr_frac_interp(xx[["apy_rice"]], 54, 53)
}


## Sesamum
## #######

xx[["apy_sesamum"]] %<>% locf_interp(61:63)

## Sorghum
## #######

xx[["apy_sorghum-kharif"]] %<>% locf_interp(61:63)
xx[["irr_sorghum-kharif"]] %<>% locf_interp(61:63)
xx[["apy_sorghum-rabi"]] %<>% locf_interp(61:63)
xx[["irr_sorghum-rabi"]] %<>% locf_interp(61:63)

## Soybean
## #######

xx[["apy_soyabean"]] %<>% locf_interp(61:63)

## Sugarcane
## #########

xx[["irr_sugarcane-whole_year"]] %<>% linear_interp(62)
## xx[["apy_sugarcane-whole_year"]] %<>% tot_frac_interp(xx[["irr_sugarcane-whole_year"]], 61:63, 60)
xx[["apy_sugarcane-whole_year"]] = xx[["irr_sugarcane-whole_year"]]

## Wheat
## #####

xx[["irr_wheat"]] %<>% linear_interp(62)
xx[["apy_wheat"]] %<>% tot_frac_interp(xx[["irr_wheat"]], 61:63, 60)

