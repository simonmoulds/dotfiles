Two directories:

code/

model.cpp
---------
the model itself, written in Rcpp (C++)

model.R
-------
R script which calls the model

run_scenarios.R
---------------
script which runs the various scenarios for each district. This is the
main script which calls all others. It should be run from the root
directory (i.e. from wrr_paper/) and is the only script which should be
run by the user. Individual scenarios can be run by commenting out those
that are not required. 

plots.R
-------
R script to make plots from the model output

config/*
--------

