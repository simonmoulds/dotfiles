#!/usr/bin/env python
# -*- coding: utf-8 -*-
#
# AquaCrop
#
import os
import numpy as np
import pcraster as pcr
import VirtualOS as vos
import netCDF4 as nc
import datetime as datetime
import calendar as calendar

class FarmParameters(object):

    def __init__(self, FarmParameters_variable):
        self.var = FarmParameters_variable
        self.var.nFarm = 5
        # self.var.FarmNumber - quantity of each representative farm
        # self.var.FallowArea - farm area left fallow
        # self.var.FarmTubewellCapacity
        # self.var.FarmCanalAccess - does the farm have access to canal water?
        # self.var.FarmCanalReliability - how reliable is the canal supply?        
        self.var.landmask_farm = np.broadcast_to(
            self.var.landmask[None,:,:],
            (self.var.nFarm, self.var.nLat, self.var.nLon))
        
    def initial(self):
        pass

    def read_farm_area(self):   # Consider separate class???
        pass
        # FarmArea = vos.netcdf2PCRobjClone(
        #     self.var.FarmAreaFileNC,
        #     'farm_area',
        #     date,
        #     useDoy = None,
        #     cloneMapFileName = self.var.cloneMap,
        #     LatitudeLongitude = True)
        # self.FarmArea = FarmArea[self.var.landmask_farm]
        
    def dynamic(self):
        self.read_farm_area()
