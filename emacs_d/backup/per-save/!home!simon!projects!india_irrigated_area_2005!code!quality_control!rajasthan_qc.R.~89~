## Author : Simon Moulds
## Date   : January 2018

## Quality control for Rajasthan

## Crops not grown: Banana, Cassava, Coconut, Coffee, Finger Millet, ...

## Barley
## ######

if (dist_nm %in% c("Jaisalmer,Jhunjhunun","Jodhpur","Nagaur","Sikar")) {
    xx[["irr_barley-rabi"]] %<>% irr_frac_interp(xx[["apy_barley-rabi"]], 11, 10)
}

if (dist_nm %in% "Jaisalmer") {
    xx[["irr_barley-rabi"]] %<>% irr_frac_interp(xx[["apy_barley-rabi"]], 5, c(4,6))
}

if (dist_nm %in% "Jalor") {
    xx[["irr_barley-rabi"]] %<>% irr_frac_interp(xx[["apy_barley-rabi"]], 7, c(6,8))
}

## chickpea
## ########

xx[["irr_chickpea-rabi"]] %<>% irr_frac_interp(xx[["apy_chickpea-rabi"]], 11, 10)

## cotton
## ######

if (dist_nm %in% c("Baran")) {
    xx[["apy_cotton-kharif"]] = xx[["mapspam_atot_cotton"]][6]
    xx[["irr_cotton-kharif"]] = xx[["mapspam_xirr_cotton"]][6]
}

## finger millet
## #############

## NB MapSPAM suggests that finger millet is grown in Rajasthan but the
## statistics suggest otherwise - stick with statistics for now

## groundnut
## #########

## all ok

## lentil
## ######

## all ok

## maize
## #####

## all ok

## other cereals
## #############

if (dist_nm %in% c("Ajmer","Baran","Ganganagar","Jaipur")) {
    xx[["apy_other_cereals-kharif"]] = xx[["mapspam_atot_other_cereals"]][6]
    xx[["irr_other_cereals-kharif"]] = xx[["mapspam_xirr_other_cereals"]][6]
}

if (dist_nm %in% c("Jalor","Sirohi")) {
    xx[["apy_other_cereals-rabi"]] %<>% tot_frac_interp(xx[["irr_other_cereals-rabi"]], 7, c(6,8))
}

## other fibre crops
## #################

if (dist_nm %in% c("Chittaurgarh","Dungarpur")) {
    xx[["apy_other_fibre_crops-kharif"]] = xx[["mapspam_atot_other_fibre_crops"]][6]
    xx[["irr_other_fibre_crops-kharif"]] = xx[["mapspam_xirr_other_fibre_crops"]][6]
}

## other oil crops
## ###############

if (dist_nm %in% c("Alwar","Dausa","Dhaulpur","Karauli","Sikar")) {
    xx[["apy_other_oil_crops-rabi"]] %<>% locf_interp(1:7, fromLast=TRUE)
    xx[["irr_other_oil_crops-rabi"]] %<>% locf_interp(1:7, fromLast=TRUE)
}

if (dist_nm %in% "Baran") {
    xx[["apy_other_oil_crops-kharif"]][4] = 0
}

if (dist_nm %in% c("Barmer","Jalor","Jodhpur")) {
    xx[["apy_other_oil_crops-kharif"]][11] = xx[["apy_other_oil_crops-rabi"]][11]
    xx[["irr_other_oil_crops-kharif"]][11] = xx[["irr_other_oil_crops-rabi"]][11]
    xx[["apy_other_oil_crops-rabi"]] = 0
    xx[["irr_other_oil_crops-rabi"]] = 0
}

if (dist_nm %in% c("Bharatpur","Dungarpur","Ganganagar","Hanumangarh","Jhunjhunun","Pali")) {
    ## xx[["apy_other_oil_crops-rabi"]] = xx[["apy_other_oil_crops-kharif"]]
    xx[["irr_other_oil_crops-kharif"]][8:11] = xx[["irr_other_oil_crops-rabi"]][8:11]
    xx[["apy_other_oil_crops-rabi"]] = 0
    xx[["irr_other_oil_crops-rabi"]] = 0
}

if (dist_nm %in% "Jaisalmer") {
    xx[["apy_other_oil_crops-kharif"]] %<>% tot_frac_interp(xx[["irr_other_oil_crops-kharif"]], 4, c(3,5))
    xx[["apy_other_oil_crops-rabi"]] %<>% tot_frac_interp(xx[["irr_other_oil_crops-rabi"]], 9, c(8,10))
    xx[["apy_other_oil_crops-rabi"]] %<>% tot_frac_interp(xx[["irr_other_oil_crops-rabi"]], 11, 10)
}

if (dist_nm %in% c("Sawai Madhopur","Tonk")) {
    xx[["apy_other_oil_crops-rabi"]] %<>% linear_interp(5:7)
    xx[["irr_other_oil_crops-rabi"]] %<>% linear_interp(5:7)
    xx[["apy_other_oil_crops-rabi"]] %<>% locf_interp(1:3, fromLast=TRUE)
    xx[["irr_other_oil_crops-rabi"]] %<>% locf_interp(1:3, fromLast=TRUE)    
}

if (dist_nm %in% "Sirohi") {
    xx[["apy_other_oil_crops-rabi"]][11] = 0
    xx[["irr_other_oil_crops-rabi"]][11] = 0
}

## other pulses
## ############

if (dist_nm %in% c("Alwar","Baran","Bharatpur","Bhilwara","Bundi","Chittaurgarh","Dausa","Jhalawar")) {
    xx[["apy_other_pulses-rabi"]] = xx[["irr_other_pulses-rabi"]]
}

if (dist_nm %in% "Banswara") {
    xx[["apy_other_pulses-rabi"]] %<>% tot_frac_interp(xx[["irr_other_pulses-rabi"]], 5, c(4,6))
    xx[["apy_other_pulses-rabi"]] %<>% tot_frac_interp(xx[["irr_other_pulses-rabi"]], 9, c(8,10))
}

if (dist_nm %in% "Dungarpur") {
    xx[["irr_other_pulses-rabi"]] %<>% irr_frac_interp(xx[["apy_other_pulses-rabi"]], 5, c(4,6))
    xx[["apy_other_pulses-rabi"]] = linear_interp(9)
    xx[["irr_other_pulses-rabi"]] = lienar_interp(9)
}

if (dist_nm %in% "Jaipur") {
    xx[["apy_other_pulses-rabi"]] = locf_interp(8:10)
    xx[["irr_other_pulses-rabi"]] = locf_interp(8:10)
}

if (dist_nm %in% "Udaipur") {
    xx[["irr_other_pulses-rabi"]] %<>% irr_frac_interp(xx[["apy_other_pulses-rabi"]], 5, c(4,6))
}

## pearl millet
## ############

## all ok

## pigeonpea
## #########

if (dist_nm %in% c("Ajmer","Bhilwara","Jaisalmer","Jodhpur","Nagaur")) {
    xx[["apy_pigeonpea-kharif"]] = xx[["mapspam_atot_pigeonpea"]][6]
    xx[["irr_pigeonpea-kharif"]] = xx[["mapspam_xirr_pigeonpea"]][6]
}

## potato
## ######

if (dist_nm %in% c("Alwar")) {
    xx[["apy_sweet_potato-whole_year"]] = locf_interp(1, fromLast=TRUE)
}

if (dist_nm %in% c("Baran","Barmer","Sirohi")) {
    xx[["apy_sweet_potato-whole_year"]] = locf_interp(10:11)
}

if (dist_nm %in% "Barmer") {
    xx[["apy_sweet_potato-whole_year"]] = linear_interp(8)
}

if (dist_nm %in% "Bhilwara") {
    xx[["apy_sweet_potato-whole_year"]] = locf_interp(11)
}

if (dist_nm %in% "Chittaurgarh") {
    xx[["apy_sweet_potato-whole_year"]] = linear_interp(10)
}

if (dist_nm %in% c("Churu","Kota")) {
    xx[["apy_sweet_potato-whole_year"]] = xx[["mapspam_atot_sweet_potato"]][6]
    xx[["irr_sweet_potato-whole_year"]] = xx[["mapspam_xirr_sweet_potato"]][6]
}

if (dist_nm %in% "Jaipur") {
    xx[["apy_sweet_potato-whole_year"]] %<>% linear_interp(4)
    xx[["apy_sweet_potato-whole_year"]] %<>% locf_interp(11)
}

if (dist_nm %in% "Jhunjhunun") {
    xx[["apy_sweet_potato-whole_year"]] %<>% locf_interp(4:5, fromLast=TRUE)
}

if (dist_nm %in% "Jodhpur") {
    xx[["apy_sweet_potato-whole_year"]] %<>% linear_interp(7)
}

## rapeseed
## ########

## all ok

## rest of crops
## #############

## all ok

## rice
## ####

if (dist_nm %in% "Ajmer") {
    xx[["irr_rice-autumn"]][1] = xx[["irr_rice-winter"]][1]
}

xx[["irr_rice-winter"]] = xx[["irr_rice-autumn"]]
xx[["irr_rice-autumn"]] = 0

## sesameseed
## ##########

## all ok

## sorghum
## #######

if (dist_nm %in% "Churu") {
    xx[["apy_sorghum-kharif"]] = xx[["mapspam_atot_sorghum"]][6]
    xx[["irr_sorghum-kharif"]] = xx[["mapspam_xirr_sorghum"]][6]
}

## soybean
## #######

if (dist_nm %in% c("Dhaulpur","Jaipur")) {
    xx[["apy_soybean-kharif"]] = xx[["mapspam_atot_soybean"]][6]
    xx[["irr_soybean-kharif"]] = xx[["mapspam_xirr_soybean"]][6]
}

## sugarcane
## #########

if (dist_nm %in% "Ajmer") {
    xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 1:4, 5)
}

if (dist_nm %in% "Baran") {
    xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 7, c(6,8))
}

if (dist_nm %in% "Kota") {
    xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 2, c(1,3))
}

if (dist_nm %in% "Sikar") {
    xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 1:2, 3)
    xx[["irr_sugarcane-whole_year"]] %<>% irr_frac_interp(xx[["apy_sugarcane-whole_year"]], 5:6, c(4,7))
}

if (dist_nm %in% "Sirohi") {
    xx[["apy_sugarcane-whole_year"]] = xx[["mapspam_atot_sugarcane"]][6]
    xx[["irr_sugarcane-whole_year"]] = xx[["mapspam_xirr_sugarcane"]][6]
}

## sunflower
## #########

if (dist_nm %in% c("Ajmer","Bharatpur","Bikaner","Chittaurgarh","Dhaulpur","Jalor","Jodhpur","Nagaur")) {
    xx[["apy_sunflower-kharif"]] = xx[["mapspam_atot_sunflower"]][6]
    xx[["irr_sunflower-kharif"]] = xx[["mapspam_xirr_sunflower"]][6]
}

if (dist_nm %in% "Jhalawar") {
    xx[["apy_sunflower-kharif"]] %<>% locf_interp(7:11)
}

## temperate fruit
## ###############

xx[["apy_temperate_fruit-whole_year"]] = xx[["mapspam_atot_temperate_fruit"]][6]
xx[["irr_temperate_fruit-whole_year"]] = xx[["mapspam_xirr_temperate_fruit"]][6]

## tropical fruit
## ##############

## NB MapSPAM suggests that tropical fruit is grown in Rajasthan but
## the statistics suggest otherwise - stick with statistics for now

## vegetables
## ##########

## as above

## wheat
## #####

if (dist_nm %in% "Barmer") {
    xx[["irr_wheat-rabi"]] %<>% irr_frac_interp(xx[["apy_wheat-rabi"]], 4, c(3,5))
}

if (dist_nm %in% c("Barmer","Jaisalmer")) {
    xx[["irr_wheat-rabi"]] %<>% irr_frac_interp(xx[["apy_wheat-rabi"]], 7, c(6,8))
    xx[["irr_wheat-rabi"]] %<>% irr_frac_interp(xx[["apy_wheat-rabi"]], 11, 10)
}

    
    
