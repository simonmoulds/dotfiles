#!/usr/bin/env python
# -*- coding: utf-8 -*-

import unittest
import numpy as np
from mock import patch, Mock, MagicMock
from CanopyCover import CanopyCover
from AquaCrop import AquaCrop

class TestCanopyCover(unittest.TestCase):

    def setUp(self):
        self.mock_aquacrop_model = Mock()
        self.mock_aquacrop_model.nCrop = 1
        self.mock_aquacrop_model.nCell = 1
        self.mock_aquacrop_model.CCprev = 1
        self.CanopyCover = CanopyCover(self.mock_aquacrop_model)
        self.CanopyCover.initial()

    def test_canopy_cover_development_growth(self):
        arr = np.ones((1, 1))
        res = self.CanopyCover.canopy_cover_development(
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)), 'Growth')
        np.testing.assert_array_almost_equal(
            res, 0.9080, decimal=4)

    def test_canopy_cover_development_decline(self):
        arr = np.ones((1, 1))
        res = self.CanopyCover.canopy_cover_development(
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)), 'Decline')
        np.testing.assert_array_almost_equal(
            res, 0.9140, decimal=4)
        
    def test_canopy_cover_required_time(self):
        res = self.CanopyCover.canopy_cover_required_time(
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)),
            np.ones((1,1)), 'CGC')
        np.testing.assert_array_compare(
            np.greater_equal, res, 0.)

    def test_adjust_CCx(self):
        pass

    def test_potential_canopy_development(self):
        pass

    def test_actual_canopy_development(self):
        pass

    def test_update_CCx_and_CDC(self):
        pass

    def test_dynamic(self):
        pass

if __name__ == '__main__':
    unittest.main()
