#!/bin/bash

# for rgn in 0.5 0.25 0.1 0.0833333333333 0.05 0.01
for rgn in 0.5
do
    rgn_str=$(printf "%0.6f" ${rgn})
    for dir in jules_land_frac jules_rivers_props
    # for dir in jules_frac jules_land_frac jules_overbank_props jules_pdm jules_rivers_props jules_soil_props jules_top
    do
	if [ ! -d ${dir} ]
	then
	    mkdir $dir
	fi
	sshpass -p "Moulds2018" scp sm510@cv-hydro2.cv.ic.ac.uk:~/projects/jules_apps/data/$dir/*${rgn_str}Deg.tif ${dir}
    done
done
