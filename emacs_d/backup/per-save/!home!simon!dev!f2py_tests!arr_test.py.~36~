#!/usr/bin/env python

import numpy as np
import mult0
import mult1


def test0():
    nrow = 1000000
    ncol = 10
    arr = np.ones((nrow, ncol), dtype=np.float64, order='F')
    arr = mult0.mult0(arr, nrow, ncol)


def test1():
    nrow = 1000000
    ncol = 10
    arr = np.ones((nrow, ncol), dtype=np.float64, order='F')
    arr = mult1.mult1(arr, nrow, ncol)


def test2():
    nrow = 10
    ncol = 1000000
    arr = np.ones((nrow, ncol), dtype=np.float64, order='F')
    arr = mult1.mult1(arr, nrow, ncol)


if __name__ == '__main__':
    # main()
    import timeit
    print(timeit.timeit("test0()", setup="from __main__ import test0", number=10000))
    print(timeit.timeit("test1()", setup="from __main__ import test1", number=10000))
    print(timeit.timeit("test2()", setup="from __main__ import test2", number=10000))
