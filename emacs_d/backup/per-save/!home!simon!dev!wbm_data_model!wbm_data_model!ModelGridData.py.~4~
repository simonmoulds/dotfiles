#!/usr/bin/env python
# -*- coding: utf-8 -*-

import os
import string
import numpy as np
import shutil

from hm import file_handling

import logging
logger = logging.getLogger(__name__)
        
class ModelGridData(object):
    def __init__(self, model):
        self.model = model
        self.model_grid_output_path = str(
            self.model._configuration.MODEL_GRID['areaMapsPathOut']
        )

    def initial(self):
        pass

    def dynamic(self):        
        # grid_cell_area_filename = str(
        #     self.var._configuration.MASK_OUTLET['gridCellAreaInputFile']
        # )
        # grid_cell_area_output_filename = str(
        #     self.model._configuration.MODEL_GRID['gridCellAreaOutputFile']
        # )
        # grid_cell_area_output_varname = str(
        #     self.model._configuration.MODEL_GRID['gridCellAreaOutputVariableName']
        # )
        # self.var.grid_cell_area = read_gdal_raster_image(
        #     grid_cell_area_filename,
        #     self.bbox,
        #     0)        
        # self.var.grid_cell_area *= 1e6  # km2 -> m2
        # netcdf = OutputNetCDF(
        #     self.var._configuration.NETCDF_ATTRIBUTES,
        #     self.var.dimensions,
        #     variable_list
        #     )        
        # netcdf.create_netCDF(
        #     grid_cell_area_output_filename,
        #     grid_cell_area_output_varname
        # )
        # netcdf.add_data_to_netcdf(
        #     grid_cell_area_output_filename,
        #     grid_cell_area_output_varname,
        #     self.var.grid_cell_area
        # )
        shutil.copy(
            str(self.model._configuration.MODEL_GRID['gridCellAreaInputFile']),
            self.model_grid_output_path
        )        
        shutil.copy(
            str(self.model._configuration.MODEL_GRID['cloneMap']),
            self.model_grid_output_path
        )    
        shutil.copy(
            str(self.model._configuration.MODEL_GRID['landmask']),
            self.model_grid_output_path
        )
