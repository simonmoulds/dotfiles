## Author : Simon Moulds
## Date   : Mar 2020

library(raster)

cropland = raster("Hybrid_10042015v9_cropped.tif")

r = raster("data/wb_cropland_pct.tif")
plot(r)

igp_fs = list.files("data", "^igp_.*.tif$", full.names=TRUE)

r = raster(igp_fs[1])
plot(r)
