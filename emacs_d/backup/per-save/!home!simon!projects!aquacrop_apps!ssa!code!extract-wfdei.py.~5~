#!/usr/bin/env python
# -*- coding: utf-8 -*-

import shutil
import os
import glob

wd = '/home/simon/projects/aquacrop_apps/ssa'

def run():
    wfdei_years = range(1979,2017)
    # wfdei_vars = [
    #     'LWdown_daily','PSurf_daily','Qair_daily',
    #     'Rainf_daily','Snowf_daily','SWdown_daily',
    #     'Tair_daily','Tair','Wind_daily'
    # ]
    wfdei_vars = ['PSurf_daily']
    try:
        os.mkdir("../data/WFDEI")
    except FileExistsError:
        pass
    for var in wfdei_vars:
        dir = os.path.join(
            '..',
            'data-raw',
            'WFDEI',
            var + '_WFDEI'
            )
        newdir = os.path.join(
            '..',
            'data',
            'WFDEI',
            var + '_WFDEI'
            )
        try:
            os.mkdir(newdir)
        except FileExistsError:
            pass
        
        fs = glob.glob(dir + '/*.gz')
        for f in fs:
            newf = os.path.join(
                newdir,
                os.path.splitext(
                    os.path.basename(f)
                )[0]
            )
            os.system("gunzip -v -c " + str(f) + " > " + os.path.splitext(f)[0])
            
if __name__ == '__main__':
    run()
            
