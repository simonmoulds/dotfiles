# TODO: header info

class soil(object):

    def __init__(self, soil_variable):
        self.var = soil_variable

    def initial(self):
        """
        Initial part of the soil module
        """

        # idea here is to load the NetCDF containing soil properties and get the 'z' dimension 
        nc = Dataset()
        self.var.soilLayers = len(nc.dimensions['z'])

        
