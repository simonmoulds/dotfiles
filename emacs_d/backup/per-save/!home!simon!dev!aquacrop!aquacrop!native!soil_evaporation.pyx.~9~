cdef extern from "c_soil_evaporation.h":
    void c_soil_evaporation(
			    double* prec, 
			    double* et_ref, 
			    double* es_act, 
			    double* e_pot, 
			    double* irr,
			    int* irr_method,
			    double* infl,
			    double* th,
			    double* th_sat,
			    double* th_fc,
			    double* th_wilt,
			    double* th_dry, 
			    double* surface_storage, 
			    double* wet_surf,
			    double* w_surf,
			    double* w_stage_two, 
			    double* cc,
			    double* cc_adj,
			    double* ccx_act, 
			    double* evap_z,
			    double* evap_z_min,
			    double* evap_z_max, 
			    double* rew,
			    double* kex,
			    double* ccxw,
			    double* fwcc,
			    double* f_evap,
			    double* f_wrel_exp, 
			    double* dz,
			    double* dz_sum, 
			    double* mulches,
			    double* f_mulch,
			    double* mulch_pct_gs,
			    double* mulch_pct_os, 
			    int* growing_season,
			    int* senescence,
			    int* premat_senes, 
			    int calendar_type,
			    int* dap,
			    int* delayed_cds,
			    int* delayed_gdds, 
			    int* time_step,
			    int evap_time_steps, 
			    int n_farm,
			    int n_crop,
			    int n_comp,
			    int n_cell)

def soil_evaporation(
                     double[:, :, :] prec,
                     double[:, :, :] et_ref,
		     double[:, :, :] es_act,
		     double[:, :, :] e_pot, 
		     double[:, :, :] irr,
		     int[:, :, :] irr_method,
		     double[:, :, :] infl,
		     double[:, :, :] th,
		     double[:, :, :] th_sat,
		     double[:, :, :] th_fc,
		     double[:, :, :] th_wilt,
		     double[:, :, :] th_dry, 
		     double[:, :, :] surface_storage, 
		     double[:, :, :] wet_surf,
		     double[:, :, :] w_surf,
		     double[:, :, :] w_stage_two, 
		     double[:, :, :] cc,
		     double[:, :, :] cc_adj,
		     double[:, :, :] ccx_act, 
		     double[:, :, :] evap_z,
		     double[:, :, :] evap_z_min,
		     double[:, :, :] evap_z_max, 
		     double[:, :, :] rew,
		     double[:, :, :] kex,
		     double[:, :, :] ccxw,
		     double[:, :, :] fwcc,
		     double[:, :, :] f_evap,
		     double[:, :, :] f_wrel_exp, 
		     double[:, :, :] dz,
		     double[:, :, :] dz_sum, 
		     double[:, :, :] mulches,
		     double[:, :, :] f_mulch,
		     double[:, :, :] mulch_pct_gs,
		     double[:, :, :] mulch_pct_os, 
		     int[:, :, :] growing_season,
		     int[:, :, :] senescence,
		     int[:, :, :] premat_senes, 
		     int calendar_type,
		     int[:, :, :] dap,
		     int[:, :, :] delayed_cds,
		     int[:, :, :] delayed_gdds, 
		     int time_step,
		     int evap_time_steps, 
		     int n_farm,
		     int n_crop,
		     int n_comp,
		     int n_cell):

    c_soil_evaporation(
                       <double*> np.PyArray_DATA(prec),
                       <double*> np.PyArray_DATA(et_ref),
		       <double*> np.PyArray_DATA(es_act),
		       <double*> np.PyArray_DATA(e_pot), 
		       <double*> np.PyArray_DATA(irr),
		       <int*> np.PyArray_DATA(irr_method),
		       <double*> np.PyArray_DATA(infl),
		       <double*> np.PyArray_DATA(th),
		       <double*> np.PyArray_DATA(th_sat),
		       <double*> np.PyArray_DATA(th_fc),
		       <double*> np.PyArray_DATA(th_wilt),
		       <double*> np.PyArray_DATA(th_dry), 
		       <double*> np.PyArray_DATA(surface_storage), 
		       <double*> np.PyArray_DATA(wet_surf),
		       <double*> np.PyArray_DATA(w_surf),
		       <double*> np.PyArray_DATA(w_stage_two), 
		       <double*> np.PyArray_DATA(cc),
		       <double*> np.PyArray_DATA(cc_adj),
		       <double*> np.PyArray_DATA(ccx_act), 
		       <double*> np.PyArray_DATA(evap_z),
		       <double*> np.PyArray_DATA(evap_z_min),
		       <double*> np.PyArray_DATA(evap_z_max), 
		       <double*> np.PyArray_DATA(rew),
		       <double*> np.PyArray_DATA(kex),
		       <double*> np.PyArray_DATA(ccxw),
		       <double*> np.PyArray_DATA(fwcc),
		       <double*> f_evap,
		       <double*> f_wrel_exp, 
		       <double*> dz,
		       <double*> dz_sum, 
		       <double*> mulches,
		       <double*> f_mulch,
		       <double*> mulch_pct_gs,
		       <double*> mulch_pct_os, 
		       <int*> growing_season,
		       <int*> senescence,
		       <int*> premat_senes, 
		       <int> calendar_type,
		       <int*> dap,
		       <int*> delayed_cds,
		       <int*> delayed_gdds, 
		       <int*> time_step,
		       <int> evap_time_steps, 
		       <int> n_farm,
		       <int> n_crop,
		       <int> n_comp,
		       <int> n_cell):


