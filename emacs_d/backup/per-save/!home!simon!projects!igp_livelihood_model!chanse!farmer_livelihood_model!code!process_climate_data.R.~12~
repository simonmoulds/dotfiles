## Author : Simon Moulds
## Date   : Jan 2020

library(tidyverse)

options(stringsAsFactors = FALSE)
climate_data_fn = file.path(
    "data",
    "input",
    "sult_base_all_data_irr2.txt"
)
climate_data =
    read.table(climate_data_fn, header=TRUE) %>%
    mutate(date = as.POSIXct(date))

rainfall_data_fn = file.path(
    "data",
    "input",
    "sult_base_sult_precip_1971_2018.txt"
)
rainfall_data = read.table(rainfall_data_fn, header=TRUE)

climate_data$time = as.POSIXlt(climate_data$time)

prec = rainfall_data[["precip"]]
reference_ET = climate_data[["ETo_mm"]]
time = as.POSIXlt(rainfall_data[["date"]])

met_data = data.frame(
    time=time,
    prec=prec,
    

## time data - taken from time attributes of climate input data
tm = as.POSIXlt(rainfall_data[["date"]]) # safe to use this in future runs as well?
## day = tm[["yday"]] + 1
## month = tm[["mon"]] + 1
## year = tm[["year"]] + 1900
day = tm$yday + 1
month = tm$mon + 1
year = tm$year + 1900
