#!/usr/bin/env python
# -*- coding: utf-8 -*-

import os
import shutil
import sys
import math
import gc
import numpy as np

from hm import file_handling
from hm.Model import Model

import logging
logger = logging.getLogger(__name__)

class WaterBalanceDataModel(Model):

    def __init__(self, config, model_time, initial_state=None):
        super(WaterBalanceDataModel, self).__init__(
            config,
            model_time,
            initial_state
        )
        self.model_grid_data = ModelGridData(self)
        # self.weather_data = WeatherData(self)
        # self.groundwater_data = GroundwaterData(self)
        # self.land_surface_data = LandSurfaceData(self)
        
    def get_model_dimensions(self):
        super(WaterBalanceDataModel, self).get_model_dimensions()
        self.n_layer = 3
        self.dimensions['depth'] = np.arange(self.n_layer)
        
    def initial(self):
        self.model_grid_data.initial()
        # self.weather_data.initial()
        # self.groundwater_data.initial()
        # self.land_surface_data.initial()
        
    def dynamic(self):
        self.model_grid_data.initial()
        # self.weather_data.dynamic()
        # self.groundwater_data.dynamic()
        # self.land_surface_data.dynamic()
