#!/usr/bin/env python
# -*- coding: utf-8 -*-

import os
import numpy as np
import rasterio

from hydro_model_builder.ncConverter import np2netCDF
from hydro_model_builder import VirtualOS as vos

import variable_list as variable_list
from utils import *

class Tubewells(object):    
    def __init__(self, Tubewells_variable):
        self.var = Tubewells_variable
        self.var.tubewellOutputFile = vos.getFullPath(
            self.var._configuration.tubewellOptions['outputFile'],
            self.var._configuration.globalOptions['outputDir'])
        self.bbox = vos.get_clone_map_extent(self.var._configuration.cloneMap)
        print self.var.tubewellOutputFile

    def initial(self):
        pass

    def dynamic(self):
        pass
