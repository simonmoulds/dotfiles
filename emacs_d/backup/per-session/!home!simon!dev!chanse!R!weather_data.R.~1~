#' Create a weather_data object.
#'
#' @param
#'
#' @return weather_data
#'
weather_data = function(precipitation, et0) {
    ## TODO: make xts object
    structure(data.frame(prec=precipitation, et0=et0))
}
